Here's a basic R code snippet that calculates the mean of a given set of numbers:  

```r
# Define a vector of numbers
numbers <- c(10, 20, 30, 40, 50)

# Calculate the mean
mean_value <- mean(numbers)

# Print the result
print(paste("The mean is:", mean_value))

